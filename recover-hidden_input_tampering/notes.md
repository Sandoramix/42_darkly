# Hidden input

on `/index.php?page=recover#` there is a hidden input with `name="mail"` and `value="webmaster@borntosec.com"`.
By changing the value of the hidden input the the flag is shown.

This doesn't make any sense, but it's a good example of how to tamper with a hidden input.